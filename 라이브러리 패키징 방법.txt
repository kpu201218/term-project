압축을 푼 폴더에서 
shift+ 오른쪽 마우스 클릭으로 여기서 명령창 열기 실행


python.exe setup.py sdist 입력
python.exe setup.py install 입력
python.exe setup.py bdist_wininst입력

클릭 후 생기는 dist 폴더의 movie DB-0.1.win32 실행

파이썬으로 선택하여 설치 진행

그러면 파이썬을 실행 시키고 import Movie_DB_startup 입력

Movie_DB_startup.main()을 입력하여 프로그램 실행

(주의 설치하는 함수 아래의 알집을 여기에 풀어야
실행이 된다)